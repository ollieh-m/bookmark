<div> <a href='/bookmarks/new'>Add Link</a> </div>

<h1> Bookmarks </h1>

<ul id='bookmarks'>
  <% @bookmarks.each do |bookmark| %>
    <li>
      Title: <%= bookmark.name %>
      URL:   <%= bookmark.url  %>
      Tags:   <%= bookmark.tags.map{|x| x.name} %>
    </li>
  <% end %>
</ul>
